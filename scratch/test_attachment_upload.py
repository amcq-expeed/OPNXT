"""Manual smoke test for attachment upload flow.

This script exercises the live API and should not run as part of automated
pytest suites. To prevent CI failures when ``scratch`` is collected, bail out
immediately. Developers can execute the script directly if they need to test
against a running backend.
"""

raise SystemExit("scratch/test_attachment_upload.py is excluded from automated test runs")
