# Test Plan

- Standard: IEEE 829 / ISO/IEC/IEEE 29119 (adapted)
- Generated: {{ now() }}

## 1. Introduction
### 1.1 Purpose
Define the objectives, resources, and processes for testing the product.

### 1.2 Scope
{{ summaries.get('Testing', '') or 'TBD' }}

## 2. Test Items
- Features under test:
{% for req in answers.get('Requirements', []) %}- {{ req }}
{% else %}- TBD
{% endfor %}

## 3. Features to be Tested
- As listed above; prioritized based on MVP and risk.

## 4. Features Not to Be Tested
- TBD

## 5. Approach
- Strategy (unit/integration/e2e): {{ (answers.get('Testing', [])[0] if answers.get('Testing')|length > 0 else 'TBD') }}
- Environments / Test Data: {{ (answers.get('Testing', [])[1] if answers.get('Testing')|length > 1 else 'TBD') }}

## 6. Pass/Fail Criteria
- Acceptance Criteria derived per feature in SRS.

## 7. Suspension Criteria and Resumption Requirements
- TBD

## 8. Test Deliverables
- Test cases, results, defect reports, coverage reports.

## 9. Testing Tasks
- TBD

## 10. Responsibilities
- Team roles: {{ (answers.get('Implementation', [])[0] if answers.get('Implementation')|length > 0 else 'TBD') }}

## 11. Schedule
- Aligned with project timeline: {{ (answers.get('Planning', [])[2] if answers.get('Planning')|length > 2 else 'TBD') }}

## 12. Risks and Contingencies
- TBD
