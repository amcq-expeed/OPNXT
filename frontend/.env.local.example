# Frontend environment variables
# Do NOT store secrets in frontend env files. The frontend uses the backend for LLM access.

# Base URL for the FastAPI backend (defaults to http://localhost:8000 if omitted)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000